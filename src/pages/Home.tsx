import { Box, Button } from '@chakra-ui/react';
import PostsContainer from '../features/posts/PostsContainer';

const Home = () => {
  return (
    <Box>
      <PostsContainer />
    </Box>
  );
};

export default Home;
